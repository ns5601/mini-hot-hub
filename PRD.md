# 今日热搜 · 产品需求文档 (PRD.md)

## 1. 产品概述
一个轻量、聚合的全网热搜看板。用户打开即用，无需注册登录，以瀑布流/网格卡片的形式快速浏览全网最热的话题。

## 2. 目标用户
- 想要在 1 分钟内快速看完吃瓜热点、行业动态的网民、上班族。
- 参与 Vibe Coding 敏捷全栈开发的学习者。

## 3. 核心功能需求（统一至 MVP）

### 3.1 首页卡片网格布局（看板）
- **卡片数量**：首期固定 3 张（微博热搜、知乎热榜、B站热搜）。
- **卡片内包含**：平台图标/名称、榜单类型名称、Top 10~20 条目列表、**“更新于 x 分钟前”的相对时间提示**。
- **热搜条目包含**：
  - 排名（1-20），其中 Top 1-3 要求视觉特殊高亮（如金银铜色或特殊背景）。
  - 标题：文字超出卡片宽度时自动截断（`text-overflow: ellipsis`）。
  - 跳转：点击热搜行，直接在**新标签页（`target="_blank"`）**打开对应的真实原厂落地页。
  - 热度（可选）：右对齐展示简短的热度文字。

### 3.2 数据接口与缓存机制
- 后端需要提供统一的 API 端点。
- 支持单个获取 `GET /api/hot/:source`，也必须支持聚合获取 `GET /api/hot`（一次性返回三个平台的数据，减少前端请求次数）。
- 统一JSON格式（见技术设计）
- **数据时效**：后端内存强缓存 5~10 分钟。前端每 1 分钟自增计算“更新于 x 分钟前”。

### 3.3 异常容灾体验
- **卡片级隔离**：如果 B 站的接口因为风控无法访问，B 站卡片应展示“数据开小差了，请稍后再试”并提供重试按钮，而微博、知乎卡片必须保持正常渲染。

### 3.4 部署
- GitHub托管代码
- 公网 HTTPS 可访问

### 3.5 响应式与页脚约束
- **PC 端**：标准 3 列网格网格布局（Grid），参考今日头条，信息清晰，卡片分区明确
- **移动端**：自动适配为 1 列纵向流式布局。
- **页脚（Footer）**：必须展示声明：“本站仅供全栈开发学习交流，数据源于网络公开接口，不进行本地存储，严禁用于任何商业用途。”

## 4. 后续规划（Backlog）
- 增加抖音热榜、百度热搜、IT之家热榜。
- 支持一键切换暗黑模式（Dark Mode）。
- 支持用户自由拖拽卡片调整顺序。

## 5. 技术栈
- 前端：React + Typescript + Vite + CSS
- 后端：Node.js + Express
- 部署：示例 Vercel + Railway，不强制统一